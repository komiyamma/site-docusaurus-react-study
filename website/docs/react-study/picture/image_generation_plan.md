# 画像生成予約リスト (Chapter 006-010)

Chapter 006-010 の記事内容に基づき、理解を助ける挿絵を計画します。
スタイルは `picture/style_guide.md` の「手書きスケッチ風」を継続します。

---

## 🎨 共通設定 (Common Settings)
*   **Style**: Friendly Hand-Drawn Sketch (Watercolor/Ink)
*   **Target Audience**: Japanese learners
*   **Text/Label Rules**: English for Code, Japanese for others (keep simple)

---

## 📘 Chapter 006: テンプレート選択 (React + TS)

### 6-1. フレームワークの選択メニュー
*   **Filename**: `react_study_006_framework_selection.png` ([x] 生成済み)
*   **Target Section**: 「フレームワークは React を選ぶ」付近
*   **Prompt**:
    ```text
    A hand-drawn illustration of a restaurant menu board or a selection screen.
    Items listed: 'Vanilla', 'Vue', 'React' (highlighted/circled), 'Svelte'.
    A hand is pointing confidently at 'React'.
    Warm, sketchy style.
    ```
*   **日本語イメージ**: メニューボードから「React」を選んでいる様子。他の選択肢（Vueなど）もある中で、Reactが強調されている。

### 6-2. TypeScriptの盾
*   **Filename**: `react_study_006_ts_shield.png` ([x] 生成済み)
*   **Target Section**: 「Variant は TypeScript を選ぶ」付近
*   **Prompt**:
    ```text
    A hand-drawn illustration of a strong but friendly shield labeled 'TS' (TypeScript).
    The shield is protecting a code file from little bug monsters.
    Symbolizing safety and type checking.
    Watercolor/sketch style.
    ```
*   **日本語イメージ**: 「TS」と書かれた盾が、バグ（小さなモンスター）からコードを守っている様子。TypeScriptの安全性（型チェック）を表現。

---

## 📘 Chapter 007: フォルダ構造

### 7-1. プロジェクトの解剖図（お家）
*   **Filename**: `react_study_007_project_structure_house.png` ([x] 生成済み)
*   **Target Section**: 「フォルダ構成」または「index.html は土台」付近
*   **Prompt**:
    ```text
    A hand-drawn illustration (cross-section) of a cute house representing a React project.
    Foundation (ground) is labeled 'index.html'.
    Entrance door is labeled 'main.tsx'.
    Living room (interior) is labeled 'App.tsx'.
    Warm, educational sketch style.
    ```
*   **日本語イメージ**: 家の断面図。土台が `index.html`、玄関が `main.tsx`、リビング（中身）が `App.tsx`。ファイルの役割関係を家で例える。

---

## 📘 Chapter 008: 開発サーバー起動

### 8-1. ホットリロードの魔法
*   **Filename**: `react_study_008_hot_reload_magic.png` ([x] 生成済み)
*   **Target Section**: 「HMR（ホットリロード）を体験しよ」付近
*   **Prompt**:
    ```text
    A hand-drawn illustration of an artist painting on a canvas.
    As soon as the artist touches the canvas with a brush, the real landscape outside the window changes instantly to match.
    Symbolizing 'Hot Module Replacement' (Instant update).
    Magical, fun vibe.
    ```
*   **日本語イメージ**: 画家がキャンバスに色を塗ると、窓の外の実際の景色も「瞬時に」変わる様子。HMR（即時反映）の魔法のような体験を表現。

---

## 📘 Chapter 009: Hello World & JSX

### 9-1. JSXブレンダー
*   **Filename**: `react_study_009_jsx_blender.png`
*   **Target Section**: 「最小の「Hello World」を描こう」のポイント付近
*   **Prompt**:
    ```text
    A hand-drawn illustration of a kitchen blender.
    Ingredients labeled 'HTML tags' and 'JS Logic' are being put inside.
    The result pouring out is 'UI'.
    Symbolizing JSX mixing HTML and JavaScript.
    ```
*   **日本語イメージ**: ミキサー（ブレンダー）に「HTMLタグ」と「JSロジック」を入れて混ぜている。出来上がったジュースが「UI」。JSXが両者を混ぜ合わせるイメージ。

### 9-2. Propsの小包
*   **Filename**: `react_study_009_props_parcel.png`
*   **Target Section**: 「小さな部品（コンポーネント）を作る」のProps解説付近
*   **Prompt**:
    ```text
    A hand-drawn illustration of a delivery person handing a parcel labeled 'Props' to a character (Component).
    The parcel has a tag saying 'name="Haruka"'.
    The character looks happy to receive the data.
    ```
*   **日本語イメージ**: 配達員が「Props」と書かれた小包をキャラクター（コンポーネント）に渡している。小包には `name="はるか"` のようなタグ。データを渡すイメージ。

---

## 📘 Chapter 010: 開発ツール

### 10-1. コンポーネントX線メガネ
*   **Filename**: `react_study_010_xray_specs.png`
*   **Target Section**: 「Components タブの使い方」付近
*   **Prompt**:
    ```text
    A hand-drawn illustration of a character wearing high-tech 'X-Ray Goggles'.
    Looking at a web page, they can see the underlying wireframe/structure (components) labeled 'Header', 'Body', 'Footer'.
    Symbolizing React Developer Tools.
    ```
*   **日本語イメージ**: X線メガネ（透視メガネ）をかけたキャラクター。ウェブページを見ると、その骨組み（コンポーネントの構造）が透けて見えている。React DevToolsのイメージ。
